<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yzhao.dao.SeckillDao">
    <update id="reduceNumber">
        UPDATE seckill
        SET `number`=`number`-1
        WHERE
        seckill_id = #{seckillId}
        AND start_time <![CDATA[ <= ]]> #{killTime}
        AND end_time >= #{killTime}
        AND `number` > 0
    </update>

    <!--<select id="queryById" resultType="com.yzhao.entity.Seckill" parameterType="long">-->
        <!--SELECT-->
        <!--`seckill_id`,-->
        <!--`name`,-->
        <!--`number`,-->
        <!--`start_time`,-->
        <!--`end_time`,-->
        <!--`create_time`,-->
        <!--FROM seckill-->
        <!--WHERE `seckill_id` = #{seckillId}-->
    <!--</select>-->
    <select id="queryById" resultType="Seckill" parameterType="long">
        SELECT
        <!--mybatis配置中有驼峰命名的转换，所以seckill_id不用写成seckillId-->
        seckill_id,
        NAME,
        number,
        start_time,
        end_time,
        create_time
        FROM
        seckill
        WHERE
        seckill_id = #{seckillId}
    </select>

    <select id="queryAll" resultType="com.yzhao.entity.Seckill">
        SELECT
          seckill_id,
          `name`,
          `number`,
          start_time,
          end_time,
          create_time
        FROM
          seckill
        ORDER BY
          create_time DESC
        LIMIT #{offset},
        #{limit}
    </select>


    <select id="killByProcedure" statementType="CALLABLE">
        call execute_seckill (
            #{seckillId, jdbcType=BIGINT, mode=IN},
            #{phone, jdbcType=BIGINT, mode=IN},
            #{killTime, jdbcType=TIMESTAMP, mode=IN},
            #{result, jdbcType=INTEGER, mode=OUT}
        )
    </select>
</mapper>